import { EncodingType } from ".";
/**
 * a class wrapping the encodeAddress and decodeAddress functions.
 */
export declare class Blech32Address {
    address: string;
    witnessVersion: number;
    blindingPublicKey: string;
    witness: string;
    private constructor();
    static from(witness: string, blindingPublicKey: string, hrp: string, witnessVersion: number): Blech32Address;
    static fromString(blechString: string, encodingType: EncodingType): Blech32Address;
}
