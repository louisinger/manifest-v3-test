export interface Context {
    namespaces: Map<string, {
        pubkey: string;
    }>;
}
export declare function findNamespaces(text: string): Array<string>;
export declare function processNamespaces(ctx: Context['namespaces'], text: string): string;
export declare function preprocessor(ctx: Context, text: string): string;
