/// <reference types="node" />
import { BIP32Interface } from 'bip32';
import { networks } from 'liquidjs-lib';
import { Slip77Interface, TinySecp256k1Interface } from 'slip77';
import { MultisigPayment } from './types';
/**
 * Create a P2MS redeemscript.
 * + conf address
 * + unconf address
 * @param keys co-signers public keys.
 * @param blindingKey
 * @param required number of signature required in multisig script.
 * @param network
 */
export declare function p2msPayment(keys: BIP32Interface[], blindingKey: Slip77Interface, required: number, network: networks.Network): MultisigPayment;
/**
 * Return a blinding key from a list of extended keys.
 * @param extendedKeys must be the first addresses of multi-sig stakeholders.
 */
export declare function blindingKeyFromXPubs(extendedKeys: BIP32Interface[], ecclib: TinySecp256k1Interface): Slip77Interface;
export declare function bip67sort(array: Buffer[]): Buffer[];
