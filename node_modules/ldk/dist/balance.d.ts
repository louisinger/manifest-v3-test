import { Output } from './types';
/**
 * reduces the set of unspents in order to compute sats grouped by assets.
 * @param unspents the utxos to reduce
 */
export declare function balances(unspents: Output[]): Record<string, number>;
